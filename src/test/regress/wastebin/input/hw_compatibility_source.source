
\c td_db
show SQL_COMPATIBILITY;
CREATE FOREIGN TABLE STR_EMPTY_COL_NOT_NULL_TD
(
 S1 VARCHAR NOT NULL,
 S2 VARCHAR2 NOT NULL,
 S3 NVARCHAR2 NOT NULL,
 S4 CHAR NOT NULL,
 S5 TSVECTOR NOT NULL,
 S6 RAW NOT NULL,
 S7 TEXT NOT NULL
)
SERVER gsmpp_server
OPTIONS(LOCATION '@abs_srcdir@/data/str_empty.csv',
FORMAT 'csv',
MODE 'private'
);

SELECT * FROM STR_EMPTY_COL_NOT_NULL_TD WHERE S1 IS NULL AND S2 IS NULL AND S3 IS NULL AND S4 IS NULL AND S5 IS NULL AND S6 IS NULL AND S7 IS NULL;
\c regression
