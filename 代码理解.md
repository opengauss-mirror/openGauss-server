# 主函数程序  
postmaster.cpp 程序是整个数据库系统的入口，它会启动数据库进程，并创建并管理连接。  
它首先读取配置文件，初始化数据库参数，并创建共享内存、信号量等系统资源。  
然后，它创建并启动后台进程，如 WAL 接收进程、预写日志进程、检查点进程等。   



# 分析器  

1）词法分析 (Lexical Analysis)
由 scan.l 文件定义规则。Flex 工具会读取 scan.l，生成 scan.cpp 词法分析器代码。  
它读取 SQL 字符串，根据预定义的正则表达式模式（如关键字、标识符、操作符、常量）将其切割成一个个 Token，并识别其类型（如 SELECT, IDENT, INTEGER）。  
例如，对于 SELECT id FROM table;，词法分析器会生成 Token 序列：SELECT (关键字), id (标识符), FROM (关键字), table (标识符), ; (终结符)。  

2） 语法分析 (Syntactic Analysis):
由 gram.y 文件定义规则。Bison 工具会读取 gram.y，生成 gram.cpp 语法解析器代码。    
它根据 Token 序列，按照定义的语法规则（BNF范式） 进行匹配和组合，构建抽象语法树（AST）。AST 的节点通常对应 SQL 中的不同子句和结构（如 SelectStmt, InsertStmt）。    
语法解析器会检查 SQL 的语法是否正确。如果不正确，会报告语法错误。


3）语义分析 (Semantic Analysis):
此阶段主要由 analyze.cpp 中的函数（如 parse_analyze）处理。  
它对 AST 进行上下文有关性质的审查，检查其中表、列、函数、表达式等的合法性（如表名、列名是否存在，类型是否匹配），并解析可能存在的二义性，最终生成查询树（Query Tree） 供后续优化器使用。


# 优化器

1. planner.c 和 planner.h  
planner.c: 这是查询优化器的核心文件，负责生成查询计划。 

主要功能:  
解析查询树并生成初始的查询计划。
调用其他模块来优化查询计划。
生成最终的执行计划。

主要数据结构:
PlannerInfo: 存储查询优化过程中的信息。
Query: 存储查询树。
Plan: 存储查询计划。

关键函数:  
planner(): 主函数，负责整个查询优化过程。
subquery_planner(): 处理子查询的优化。
make_one_rel(): 生成单个关系的查询计划。
make_joins(): 处理连接操作的查询计划。
planner.h: 头文件，包含查询优化器相关的声明和定义。

主要声明:  
结构体定义，如 PlannerInfo，用于存储查询优化过程中的信息。
函数声明，如 planner() 和 subquery_planner()。


2. initsplan.c 和 initsplan.h  
initsplan.c: 用于初始化查询计划。  

主要功能:  
创建和初始化 PlannerInfo 结构体。
设置查询的基本信息和参数。
创建初始的关系节点（RelOptInfo）。
initsplan.h: 头文件，包含初始化查询计划相关的声明和定义。

主要声明:
结构体定义，如 PlannerInfo。
函数声明，如 init_planner_info()。


3. pathnode.c 和 pathnode.h
pathnode.c: 用于创建和处理路径节点（Path）。

主要功能:
创建不同类型的路径节点，如顺序扫描路径（SeqScanPath）、索引扫描路径（IndexScanPath）等。
计算路径的成本。
选择最优路径。
pathnode.h: 头文件，包含路径节点相关的声明和定义。

主要声明:
结构体定义，如 Path、SeqScanPath、IndexScanPath 等。
函数声明，如 create_seqscan_path() 和 create_indexscan_path()。


4. costsize.c 和 costsize.h  
costsize.c: 用于计算路径的成本和大小。  

主要功能:  
计算不同操作的成本，如顺序扫描、索引扫描、连接操作等。  
评估查询计划的性能。
costsize.h: 头文件，包含成本和大小计算相关的声明和定义。

主要声明:
函数声明，如 cost_seqscan() 和 cost_indexscan()。

5. relnode.c 和 relnode.h
relnode.c: 用于处理关系节点（RelOptInfo）。

主要功能:
创建和初始化关系节点。
处理关系节点的属性和约束。
生成关系节点的路径。
relnode.h: 头文件，包含关系节点相关的声明和定义。

主要声明:
结构体定义，如 RelOptInfo。
函数声明，如 create_rel()。


6. joininfo.c 和 joininfo.h
joininfo.c: 用于处理连接信息（JoinInfo）。

主要功能:
创建和初始化连接信息。
评估连接的成本和选择最优连接方式。
处理连接操作的约束和条件。
joininfo.h: 头文件，包含连接信息相关的声明和定义。

主要声明:
结构体定义，如 JoinInfo。
函数声明，如 make_joininfo_from_restrictinfo()。


7. selectivity.c 和 selectivity.h
selectivity.c: 用于计算选择性（selectivity）。

主要功能:
计算查询条件的选择性，即满足条件的行占总行数的比例。
评估查询条件对查询计划的影响。
selectivity.h: 头文件，包含选择性计算相关的声明和定义。

主要声明:
函数声明，如 clauselist_selectivity()。


8. rewrite.c 和 rewrite.h
rewrite.c: 用于重写查询树。

主要功能:
对查询树进行重写，以优化查询的结构。
处理视图和子查询的展开。
优化查询的逻辑表达式。
rewrite.h: 头文件，包含查询重写相关的声明和定义。

主要声明:
函数声明，如 rewriteQuery()。


9. rules.c 和 rules.h
rules.c: 用于处理规则系统。

主要功能:
处理数据库中的规则（rules），即自动触发的 SQL 语句。
评估规则的应用条件和执行规则。
rules.h: 头文件，包含规则系统相关的声明和定义。

主要声明:
结构体定义，如 RewriteRule。
函数声明，如 apply_query_rewrite()。


10. prep.c 和 prep.h
prep.c: 用于准备查询执行。

主要功能:
准备查询执行所需的参数。
处理查询中的常量和变量。
生成执行计划的中间表示。
prep.h: 头文件，包含查询准备相关的声明和定义。

主要声明:
函数声明，如 preprocess_query()。


11. predtest.c 和 predtest.h
predtest.c: 用于测试查询条件。

主要功能:
测试查询条件的真值。
评估条件对查询结果的影响。
处理条件中的复杂逻辑。
predtest.h: 头文件，包含条件测试相关的声明和定义。

主要声明:
函数声明，如 test_predicate()。


12. subselect.c 和 subselect.h
subselect.c: 用于处理子查询。

主要功能:
评估子查询的成本和选择最优的子查询执行方式。
处理子查询的结果和将其集成到主查询中。
优化子查询的逻辑结构。
subselect.h: 头文件，包含子查询处理相关的声明和定义。

主要声明:
函数声明，如 make_subselect()。


13. var.c 和 var.h
var.c: 用于处理变量。

主要功能:
处理查询中的变量引用。
评估变量的值和其在查询中的位置。
优化变量的使用和访问。
var.h: 头文件，包含变量处理相关的声明和定义。

主要声明:
函数声明，如 get_variable_value()。


#  执行引擎

#  存储引擎


#  参考文档
1. flex 官网：https://github.com/westes/flex  
Flex 是一个用于生成词法分析器（Lexer 或 Scanner） 的工具。它是一个代码生成器，而不是一个独立的程序。  
它的核心工作原理是：  
输入：你编写一个 .l 或 .lex 文件，其中基于正则表达式定义了各种词法规则（即如何将输入的字符流切分成一个个有意义的单词，称为 Token）。
输出：Flex 读取这个文件，并生成一个完整的 C（或 C++）源代码文件（通常是 lex.yy.c 或 lexyy.c）。这个生成的 C 文件就是一个功能完备的词法分析器。

作用：生成的词法分析器会读取一段文本（例如源代码、配置文件、SQL 语句），将其分割成一个个的 Token，并交给下一个阶段（通常是语法分析器，如由 Bison 生成的解析器）处理。

简单比喻：Flex 就像是一个为文本设计的高级“单词切割机”的工厂。你告诉工厂切割规则（例如：遇到连续数字就切下来标记为“数字”，遇到 “if” 就切下来标记为“关键字”），工厂就会给你一台专门按这个规则切割文本的机器。

2. bison 官网：https://www.gnu.org/software/bison/  
它的核心工作原理是：  
输入：你编写一个 .y 或 .yacc 文件，其中基于 BNF（巴科斯范式）定义了各种语法规则（即如何将 Token 序列组合成一个个有意义的语法结构，称为语法树）。
输出：Bison 读取这个文件，并生成一个完整的 C（或 C++）源代码文件（通常是 y.tab.c 或 ytab.c）。这个生成的 C 文件就是一个功能完备的语法分析器。

作用：生成的语法分析器会读取一段 Token 序列（例如由 Flex 生成的词法分析器生成的），按照定义的语法规则（BNF）进行匹配和组合，构建抽象语法树（AST）。AST 的节点通常对应 SQL 中的不同子句和结构（如 SelectStmt, InsertStmt）。

简单比喻：Bison 就像是一个为 Token 序列设计的高级“语法分析器”的工厂。你告诉工厂语法规则（例如：如果看到 “SELECT” 就认为接下来是一个 SelectStmt，如果看到 “FROM” 就认为接下来是一个 FromClause），工厂就会给你一台专门按这个规则分析 Token 序列的机器。  